{
  "name": "mdb_server",
  "version": "1.0.0",
  "description": "- [ ] Extract `database.js` to `config/database.js`\r - [ ] Move all model definitions (e.g., Report) to `/models`\r - [ ] Move all business logic to `/services`\r - [ ] Move all route handlers to `/controllers`\r - [ ] Define routes in `/routes` using Express Router\r - [ ] Keep `server.js` clean (only app setup & middlewares)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.14.1"
  }
}
